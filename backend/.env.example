PORT=8000
HOST=127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://path-finder-liart.vercel.app
# For PostgreSQL (e.g. Neon.tech), uncomment and set DATABASE_URL:
# DATABASE_URL=postgresql://user:password@ep-host.region.pooler.neon.tech/dbname?sslmode=require
# Fallback local SQLite path (if DATABASE_URL is not set):
PATHFINDER_DATABASE=../database/pathfinder.db
PATHFINDER_PDF_DIR=../pdfs
